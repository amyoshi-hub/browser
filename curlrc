# デフォルトで進捗バーを無効化
-s

# 出力ファイルのデフォルト名を指定
-o output.txt

# HTTP ヘッダーをデフォルトで追加
-H "User-Agent: CustomUserAgent/1.0"

# タイムアウトを設定（秒）
--connect-timeout 10

# 自己署名証明書を許可
-k

# ログイン情報のデフォルト設定
-u username:password

# リダイレクトを許可
-L

# プロキシを指定
--proxy http://proxy.example.com:8080

# クッキーを保存/読み込みする
--cookie-jar cookies.txt
--cookie cookies.txt

